# 运行时依赖 — 锁定精确版本以保证可复现构建。
# 升级时请同步更新此处版本号并重新跑测试（pip install -r requirements.txt）。
apprise==1.11.0
openai==2.41.1
efinance==0.5.8
akshare==1.18.64
apscheduler==3.11.2
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.1
pyyaml==6.0.3
certifi==2026.5.20
pytest==9.1.0
tenacity==9.1.4
fastapi==0.136.3
uvicorn==0.49.0
watchfiles==1.2.0
sqlalchemy==2.0.50
playwright==1.57.0
PyJWT==2.13.0

# X (Twitter) 舆论采集 — 无需 API Key，免费抓取
twikit

# 深度分析 — TradingAgents (多 Agent 投资决策框架, 76k star)。
# 不在 PyPI,用 git+ URL 安装。会拉 langchain/langgraph/yfinance 等较重依赖
# (~115 个包,首次安装 2-5 分钟)。
# 用户启用「TradingAgents 深度分析」Agent 时才会真正调用它;不启用零开销。
# 注: 上游用 @main 跟随最新; 如需完全可复现可改为固定 commit hash。
tradingagents @ git+https://github.com/TauricResearch/TradingAgents.git@main